/**
 * Test fixtures for mock data
 *
 * This barrel file exports all test fixtures for convenient importing.
 * Fixtures are used in tests to avoid coupling with production data.
 *
 * Usage:
 * ```typescript
 * import { mockPhotoSessions, mockShoots, mockSketchup } from '@/tests/fixtures';
 * ```
 */

export { mockPhotoSessions } from './photoSessions';
export { mockShoots } from './shoots';
export { mockSketchup } from './sketchup';
